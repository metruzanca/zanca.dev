# Site
SITE_DOMAIN=metru.dev

# GitHub (build-time snapshot + runtime refresh; falls back to `gh` CLI auth)
GITHUB_TOKEN=

# Last.fm
LASTFM_API_KEY=
LASTFM_USERNAME=

# ATProto (defaults to metru.dev)
ATPROTO_HANDLE=
